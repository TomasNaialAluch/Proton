/** Evento: el sentinel bajo el header dejó de ser visible → nav condensado en desktop. */
export const PUBLIC_NAV_CONDENSE_EVENT = "proton:publicNavCondense";

export type PublicNavCondenseDetail = { condensed: boolean };
