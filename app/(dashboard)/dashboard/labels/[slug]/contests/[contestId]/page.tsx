"use client";

import ContestDetailClient from "./ContestDetailClient";

/** Sin prop `params`: slug/contestId salen del `pathname` en el cliente (mismo patrón que labels/[slug]). */
export default function ContestDetailPage() {
  return <ContestDetailClient />;
}
