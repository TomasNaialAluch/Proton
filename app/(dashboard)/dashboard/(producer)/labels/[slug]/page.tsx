"use client";

import LabelProfileClient from "./LabelProfileClient";

/** Sin prop `params`: el slug sale del `pathname` en el cliente (mismo patrón que royalties/[id]). */
export default function LabelProfilePage() {
  return <LabelProfileClient />;
}
